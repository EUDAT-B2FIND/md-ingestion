# Run the b2f harvest jobs
# copy to /etc/cron.d/b2f
SHELL=/bin/bash
PATH=/sbin:/bin:/usr/sbin:/usr/bin
MAILTO=b2f
01 01 * * * b2f /home/b2f/bin/b2f --silent combine -c all --fromdays 1 -i host --auth key
